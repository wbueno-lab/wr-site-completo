{
  "orgId": "your-org-id",
  "projectId": "your-project-id",
  "settings": {
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "framework": "vite"
  }
}
